# 12주 로드맵 범위 결정

Task 11은 `request.json#/availableHoursPerWeek`, `result.json#/roleCandidates`, `result.json#/evidenceGaps`, `result.json#/weeks`, `result.json#/firstPortfolioBrief`만 근거로 사용한다.

기존 PDF 준비 상태와 시각화 렌더 상태가 blocked였다는 사실은 유지한다. Task 11의 독립 생성 성공은 그 과거 상태를 소급해 terminal success로 바꾸지 않는다.
