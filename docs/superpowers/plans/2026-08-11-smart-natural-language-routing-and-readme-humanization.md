# Smart Natural-Language Routing and README Humanization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 처음 설치한 사용자가 `@Game Design Studio` 또는 `@Game Design Career` 뒤에 원하는 일을 한 문장으로 적어 시작하고, README만 읽어도 플러그인이 고르는 스킬·검토 역할·결과물·사람 승인 흐름을 이해하게 한다.

**Architecture:** 제품 원천의 오케스트레이터가 자연어 요청을 분류해 단일 분야는 전문 스킬로, 복합·불명확 요청은 최소 스킬 체인으로 보낸다. 루트 README는 자연어 기본 사용법을 먼저 설명하고 사례 ID와 명시적 CLI 호출은 고급 제어로 분리한다. Diagram Design은 첫 결과의 분기 흐름을 독립 HTML/SVG/PNG로, Skillstead는 Studio·Career·연계 사례 묶음의 자동 선택 흐름을 SVG/PNG로 제공한다.

**Tech Stack:** Markdown, Node.js 18+, Node test runner, JSON, Diagram Design 2.0, Skillstead 0.8.3, headless Chromium, repository build/guide/catalog validators

## Global Constraints

- 구현 기준 설계는 `docs/superpowers/specs/2026-08-11-smart-natural-language-routing-and-readme-humanization-design.md`다.
- 작업 경로는 `/Users/freelife/game/gamedesign-plugin/.worktrees/readme-use-case-navigation`이다.
- 사용자는 일반적인 App 작업에서 사례 ID나 스킬 이름을 몰라도 된다.
- 한 분야 요청은 전문 스킬로, 복합·불명확 요청은 제품 오케스트레이터로 보낸다.
- 사용자가 경로를 명시하면 그 선택을 존중하되 안전·권리·사람 승인 경계는 유지한다.
- 자동 스킬 선택은 자동 승인을 뜻하지 않는다.
- 결과 보고에는 선택한 스킬, 선택한 검토 역할, 결과 파일, 남은 결정을 표시한다.
- 18개 대표 사례 ID, 30개 설치 스킬, 18개 에이전트와 기존 결과 파일 계약은 유지한다.
- 146개 상세 요청문 카탈로그의 ID와 구조는 바꾸지 않는다.
- 루트 README는 한국어 설명을 먼저 쓰고 영문 ID·계약어는 괄호나 코드 표기로 보조한다.
- `humanize-korean` 윤문은 5,000자 이하 섹션 단위로 수행하며 링크, ID, 명령, 수치와 안전 의미를 보존한다.
- 기본 App 예시에는 사례 ID, 명시적 스킬 체인, `fact/inference/recommendation`을 요구하지 않는다.
- 고급 CLI 예시는 접힌 영역에서 권장 진입 스킬 하나와 선택 후속 작업을 설명한다.
- 복사 가능한 요청문의 한 줄은 Unicode code point 기준 80자를 넘지 않는다.
- Diagram Design 결과는 self-contained HTML, editable SVG, 정확한 2× PNG를 모두 보존한다.
- Skillstead 결과는 editable SVG와 정확한 2× PNG를 함께 보존한다.
- 모든 도식은 한국어 우선, 한글 글꼴 대체 목록, 이름 있는 사람의 승인·보류 게이트를 가진다.
- 도식의 글자 찌그러짐, 흐림, tofu, clipping, overflow, 겹침, 중간 토큰 분할, head-only arrow는 실패다.
- 대각선·겹친 연결선·공유 부착점·화살표 위 라벨은 사용하지 않는다.
- `products/<product>/plugin/`만 원천으로 편집하고 `plugins/<product>/`는 `npm run build`로 생성한다.
- main worktree의 미추적 `package-lock.json`은 건드리지 않는다.
- 새 dependency는 추가하지 않는다.

---

## File Structure

### Create

- `guides/assets/readme/first-result-routing-flow.html`: Diagram Design 기반 첫 결과 분기 흐름 원본
- `guides/assets/readme/first-result-routing-flow.svg`: README용 editable 정적 원본
- `guides/assets/readme/first-result-routing-flow.png`: 정확한 2× README 미리보기
- `guides/assets/readme/studio-smart-request-flow.svg`: Studio 자연어 자동 선택 Skillstead 원본
- `guides/assets/readme/studio-smart-request-flow.png`: 위 도식의 2× PNG
- `guides/assets/readme/career-smart-request-flow.svg`: Career 자연어 자동 선택 Skillstead 원본
- `guides/assets/readme/career-smart-request-flow.png`: 위 도식의 2× PNG
- `guides/assets/readme/suite-smart-request-flow.svg`: Studio·Career 연계 Skillstead 원본
- `guides/assets/readme/suite-smart-request-flow.png`: 위 도식의 2× PNG

### Modify

- `README.md`: 자연어 우선 사용법, 세 사례 묶음의 간단 요청, 18개 카드 윤문, 고급 CLI 분리, 네 도식 연결
- `tests/contracts/root-readme-user-guides.test.mjs`: 자연어 기본 사용·카드·도식·사람 승인 계약
- `products/game-design-studio/plugin/skills/orchestrate-game-design-project/SKILL.md`: 자연어 자동 라우팅과 결과 보고 계약
- `products/game-design-career/plugin/skills/orchestrate-game-design-career/SKILL.md`: 자연어 자동 라우팅과 결과 보고 계약
- `products/game-design-studio/plugin/README.md`: 기본 자연어 사용과 고급 직접 호출 경계
- `products/game-design-career/plugin/README.md`: 기본 자연어 사용과 고급 직접 호출 경계
- `products/game-design-studio/plugin/.codex-plugin/plugin.json`: 기본 요청 안내가 필요할 때만 자연어 우선 문구 보강
- `products/game-design-career/plugin/.codex-plugin/plugin.json`: 기본 요청 안내가 필요할 때만 자연어 우선 문구 보강
- `tests/products/studio/product-contract.test.mjs`: Studio 라우팅·선택 공개 계약
- `tests/products/studio/readme.test.mjs`: Studio 자연어 기본 사용 계약
- `tests/products/career/readme.test.mjs`: Career 자연어 기본 사용 계약
- Career 오케스트레이터 계약을 소유한 기존 `tests/products/career/*.test.mjs`: 최소 체인·역할·선택 공개 계약
- `guides/archify-diagrams/catalog.json`: README source digest가 바뀔 때 정합성 갱신

### Generated by standard build only

- `plugins/game-design-studio/**`
- `plugins/game-design-career/**`
- 각 제품 `BUILD-MANIFEST.json`

---

### Task 1: Lock smart natural-language routing contracts

**Files:**
- Modify: `tests/contracts/root-readme-user-guides.test.mjs`
- Modify: Studio/Career product contract tests selected after repository inspection
- Reference: both orchestrator `SKILL.md` files and Studio `references/routing.json`

- [ ] **Step 1: Add RED root README contracts**

Require the five-minute section to explain that a one-line App request is enough, the plugin selects the route, and selected skills/roles/results are reported. Reject basic examples containing case IDs, explicit skill chains, or English-only `fact/inference/recommendation` fields.

- [ ] **Step 2: Add RED representative-card contracts**

Require exactly these Korean labels in all 18 cards: `바로 써 보는 요청`, `바로 복사해 쓰는 App 예시`, `경로를 직접 지정하는 CLI 예시`, `플러그인이 선택하는 작업 순서`, `받게 되는 결과`. Require simple App examples before the advanced CLI block.

- [ ] **Step 3: Add RED product routing contracts**

Require both products to preserve: direct specialist routing, mixed/unknown orchestrator routing, explicit user choice precedence, at most three necessary review roles, selected route disclosure, and no automatic approval.

- [ ] **Step 4: Prove RED**

Run focused root, Studio, and Career tests. Confirm failures identify the missing natural-language contract rather than syntax, fixture, or `TypeError` failures.

### Task 2: Make both products natural-language-first

**Files:**
- Modify: both orchestrator `SKILL.md` files
- Modify: both product `README.md` files
- Modify: plugin manifests only if their current default prompt contradicts the new contract
- Modify: focused product tests from Task 1

- [ ] **Step 1: Document the routing decision in each orchestrator**

Add the exact user-facing behavior: infer goal and desired result; route a clear single domain to its specialist; use the orchestrator for mixed or unclear work; ask only materially branching questions; honor explicit skill choice; report the selected route and artifacts.

- [ ] **Step 2: Separate default and advanced usage in product READMEs**

Show a one-line App request first. Place direct skill commands under an advanced-control heading for reproducibility, partial resume, fixed review order, and testing.

- [ ] **Step 3: Keep safety and approval invariant**

State that missing facts stay `미정`, private or unapproved material is not requested, generated derivatives are not automatically approved, and a named person decides approve/revise/hold.

- [ ] **Step 4: Prove GREEN and regenerate snapshots**

Run focused product tests, then `npm run build`. Inspect the generated plugin diff and reject unrelated snapshot churn.

### Task 3: Humanize and simplify the root README

**Files:**
- Modify: `README.md`
- Modify: `tests/contracts/root-readme-user-guides.test.mjs`
- Modify: `guides/archify-diagrams/catalog.json` when the README digest changes

- [ ] **Step 1: Rewrite the five-minute start section**

Lead with one Studio and one Career natural-language request. Explain in plain Korean what the plugin selects, when it asks a question, what it returns, and where human approval remains.

- [ ] **Step 2: Add simple-prompt tables before the three case groups**

For Studio 7, Career 7, and Suite 4 groups, provide `하려는 일`, `간단한 요청`, `플러그인이 고르는 대표 경로`, `받게 되는 결과`. The route column must say that only needed skills run.

- [ ] **Step 3: Rewrite all 18 cards**

Replace awkward labels, use realistic Korean examples, put the basic App request first, and move explicit commands/case IDs into an advanced `<details>` block. Preserve source IDs, output order, review boundary, and resume semantics.

- [ ] **Step 4: Run section-by-section Korean humanization**

Review each sub-5,000-character section for translationese, repetitive sentence openings, needless English, dense noun lists, mechanical parallelism, and internal jargon. Preserve all stable identifiers and commands.

- [ ] **Step 5: Prove README contracts GREEN**

Run focused root tests, scan fenced prompt line lengths, validate all internal links and update the catalog digest.

### Task 4: Build the Diagram Design first-result flow

**Files:**
- Create: `guides/assets/readme/first-result-routing-flow.{html,svg,png}`
- Modify: `README.md`
- Modify: `tests/contracts/root-readme-user-guides.test.mjs`

- [ ] **Step 1: Add RED semantic and asset contracts**

Require exact decision branches, specialist and orchestrator convergence, optional review-role skip, canonical result creation, named-human approval, and hold/resume. Require the PNG embed to link to the HTML, HTML to be self-contained, and PNG dimensions to be exactly 2× SVG dimensions.

- [ ] **Step 2: Author with Diagram Design 2.0**

Use an orthogonal flowchart with Korean-first labels and secondary IDs. Keep one primary left-to-right path, reserve a separate return lane for resume, and avoid shared connector attachment points.

- [ ] **Step 3: Render and inspect**

Render with headless Chromium, run SVG/HTML structural checks, then inspect fit and original PNG views. Any tofu, clipping, overlap, unclear decision direction, weak arrow, or unreadable text fails the task.

- [ ] **Step 4: Embed below the five-minute explanation**

Add a short sentence explaining what the diagram answers and link the PNG to the interactive HTML.

### Task 5: Build three Skillstead smart-request diagrams

**Files:**
- Create: `guides/assets/readme/{studio,career,suite}-smart-request-flow.{svg,png}`
- Modify: `README.md`
- Modify: `tests/contracts/root-readme-user-guides.test.mjs`

- [ ] **Step 1: Add RED graph contracts**

Require exact ordered nodes, edges, representative Korean skill names with English IDs, `요청에 따라 선택`, `필요한 경로만 실행`, named-human approval/hold, and dashed resume paths.

- [ ] **Step 2: Author through the packaged Skillstead renderer**

Create one diagram per case-group introduction. Keep body and captions at least 16px, use natural-width text without `textLength` or scaling transforms, and wrap only at semantic whitespace or hyphens.

- [ ] **Step 3: Render all three at exact 2× size**

Run the packaged Skillstead lint and canonical headless renderer. Verify deterministic byte output on a second check.

- [ ] **Step 4: Perform visual QA**

Inspect all three PNGs in fit and original views. Reject distortion, blur, tofu, clipping, title/pill collision, mid-token splitting, unreadable footer text, and head-only arrows.

- [ ] **Step 5: Embed each diagram before its detailed cards**

Explain that the path is selected per request and that users do not need to type every skill shown.

### Task 6: Integrate, verify, and finish the branch

**Files:**
- Verify all modified and generated files

- [ ] **Step 1: Run focused verification**

Run root README contracts, Studio product contracts, Career product contracts, Diagram Design asset tests, and Skillstead graph/visual contracts.

- [ ] **Step 2: Run repository validation**

Run:

```text
npm run validate:guides
npm run validate:archify-catalog
npm run check:guide-diagrams
npm run build -- --check
npm run validate
npm test
git diff --check
```

- [ ] **Step 3: Review the final documentation as a new user**

Confirm that a reader can start with one line, understands what the plugin selects, can find advanced direct controls, knows the returned files, and sees where a person approves or resumes work.

- [ ] **Step 4: Review all new diagrams at original resolution**

Record a per-file PASS/FAIL for text clarity, geometry, connectors, Korean-first terminology, and approval/resume semantics. Regenerate any failed diagram before continuing.

- [ ] **Step 5: Commit in reviewable units**

Commit product routing contracts, README content, Diagram Design assets, Skillstead assets, and final validation fixes separately. End with a clean worktree and no pending required task.

---

## Plan Self-Review

- The plan covers every approved design section: natural-language routing, README labels, 18 simple examples, three group tables, one Diagram Design flow, three Skillstead flows, product contracts, human approval, and full verification.
- Every created asset has a source, renderer, contract, README placement, and visual QA step.
- Product source and generated plugin boundaries are explicit; no generated tree is edited by hand.
- Existing case IDs, skill IDs, result files, prompt catalog structure, safety, rights, and approval contracts remain stable.
- Test tasks require non-vacuous RED failures before implementation and focused GREEN evidence before broader validation.
- No placeholder task, unresolved signature, new dependency, destructive action, browser interaction, or external publication is required.
