# Task 7 report — cutscene user guides and prompt templates

- Status: source/docs complete; generated plugin snapshot refresh remains Task 8 ownership.
- RED: the cutscene How-to did not exist. Studio documentation still exposed 23 installed skills and 25 top-level scripts, while the frozen contract requires routing 23, installed 24, and 30 scripts. Product READMEs, lifecycle tests, and guide inventory tests therefore failed on the old values.
- GREEN: added the Korean cutscene How-to with ten ordered copyable requests, Prompt Only, Estimate Only, Generate After Approval, serial `style-master → reference-masters → keyframes → storyboard` dispatch, current named approval, unavailable-price/provider-0 stop, retry, dialogue-only overlay, visual derivative, and continuity-gate boundaries. Estimate drafting may occur early; paid dispatch requires the current wave's current estimate and named live approval. `style-master` has no predecessor and dispatches before completion; later waves also require completed predecessors. In particular, keyframe completion gates only storyboard generation-ready binding and paid dispatch: storyboard planning, review, and estimate drafting can begin earlier.
- GREEN: Studio now documents the 24-skill/30-script inventory while Career remains absent from the cutscene route and stays 23 skills/30 scripts. Root, guide, Studio and product README paths, isolated inventory checks, lifecycle checks, and mutation-sensitive guide tests use those counts.
- Discovered integration gap: Task 6 added a routed Studio skill but did not supply the normal skill guide or prompt-template coverage expected by the source catalog. This Task 7 repair adds the skill guide, workbench and canonical route rows, one source shard, and beginner/standard/advanced prompt cards. The advanced card is mutation-tested by removing each required input: `current wave`, `count`, `provider`, `model`, `quality`, `size`, `USD min/expected/max`, `finite cap`, `retryReserve`, `pricing time`, `costStatus`, and `named approval`. It also rejects card-order, source-reference, and source-projection mutations.
- Prompt generator: `node tooling/build-prompt-guides.mjs --check` reports 91 Markdown and two source projections current. Its reusable summary now uses the same deterministic `level: title` separator for every catalog entry, so all 32 managed source skill-guide summaries were regenerated without hand edits. It does not write `plugins/`.
- Archify: no new Archify source, asset, or catalog digest is required. The cards bind the existing image-planning SVG/PNG because this task adds a workflow guide rather than a new visual artifact.
- Cost boundary: this task makes no provider or live image call. Real generation remains blocked until an exact current-wave estimate and named approval are supplied.
- Humanize Korean: the Fast Path limit is 5,000 characters, so the 5,828-character skill guide was split into two logical chunks; How-to and three source-card human-facing prose chunks were separate. Ignored evidence lives at `_workspace/2026-08-15-002/final.md` through `_workspace/2026-08-15-007/final.md`; each `final.md` contains its complete final body before the HTML summary. The keyframe wording repair refreshed `002`: 3,863 original characters, 3,934 final characters, and fallback Levenshtein distance `78 / max(3863, 3934) × 100 = 1.98%`. The installed skill lacks `scripts/verify_change_rate.py`; each artifact therefore records that deterministic fallback, original/final counts, tracked categories, 6/6 checklist, grade, and highlights. The body-count check matches every recorded final count: 3,934, 2,727, 3,101, 1,499, 1,959, and 2,584 characters. Actual chunk rates are 1.98%, 18.89%, 3.48%, 0.07%, 0.15%, and 10.14%; all are at or below 30%. The generated prompt page is derived from, and quick-rule-tested with, the source cards rather than separately humanized.
- Validation state: current source-focused checks are 216/216. The planned full matrix is 224/228, with only four stale generated-plugin snapshot failures: the package snapshot assertions for Career and Studio, the Studio source-plugin install lifecycle count (23 instead of 24), and isolation smoke's Studio snapshot count (23 instead of 24). These are Task 8 handoffs, not source GREEN results.
- Known handoff: `tests/contracts/package-contents.test.mjs` correctly detects that `plugins/*` snapshots are stale against the updated source projection. Refreshing those generated plugin snapshots is explicitly Task 8 ownership and was not performed here.
